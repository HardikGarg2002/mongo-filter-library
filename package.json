{
  "name": "filter-library",
  "version": "2.0.0",
  "description": "a library to make filtering easy in get api request",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hardik garg",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}